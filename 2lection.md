Code style:
snake_case or camelCase or PascalCase

встроенные типы данных:
char

int, short, long - целочисленные типы данных 

float, double

bool

void

nullptr_t

## Модификаторы
short, long
signed, unsigned

numeric_limits (#include <limits>) - библиотека для определения крайних значений типов данных
#include <cstdint> - закрепленный размер для целочисленных типов,  например: int8_t, int32_t

## Целочисленные литералы
int a = 162;
int b = 0242; // OCT
int c = 0xA2; // HEX
int d = 0b010100010; // BIN

## Вещественные литералы
double a = 0.15;
float b = 0.15f;

## представление чисел в памяти
Целые числа
    прямой код
    обратный код
    дополнительный код
Вещественные
    Знак, порядок, мантисса

## Символьные литералы
Символьный литерал -  'x'
esc-последовательность - '\n', '\t', \'(символ кавычки)

## Строковые литералы
Строка - массив символов
В конце спец символ конца строки '\0'


## Enum (перечислимый тип)
enum Color {
    RED,
    GREEN, 
    BLUE,
};

Color color = Color::BLUE;

## Объявдение переменных (declaration)
int a;
float b;
char c;

int a,b,c;

## определение переменных (definition)


## операторы
арифмитические (+, )


## преобразование типов
неявные преобразования:
    компилятор стремится меньшие типы к большим
    пример: целые --> к вещественным
явные преобразования: (c-style cast)(тип) переменная

## sizeof
- сколько определенный тип или переменная занимает байт памяти

## оператор
а + с = 0; - оператор

